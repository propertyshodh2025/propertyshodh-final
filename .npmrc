# Enable pre/post scripts
enable-pre-post-scripts=true
auto-install-peers=true

# Allow scripts for Vercel deployment
ignore-scripts=false

# Vercel-specific settings
shame-unsafe-perm=true
fund=false
audit=false

# Allow build scripts for these specific packages needed by the app
# This prevents the "Ignored build scripts" warning on Vercel
optional=false

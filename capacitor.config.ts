import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.propertyshodhfinal',
  appName: 'propertyshodh-final',
  webDir: 'dist'
};

export default config;

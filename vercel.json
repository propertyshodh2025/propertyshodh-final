{
  "framework": "vite",
  "buildCommand": "pnpm install --force && pnpm run build",
  "installCommand": "pnpm install --force",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
